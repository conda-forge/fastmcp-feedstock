# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  name: fastmcp
  version: 3.3.0

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/fastmcp-${{ version }}.tar.gz
  sha256: 48c7fffdb6865cb9658ac02c2ff589caaaa3cd68e2cc37ed51402fa46e46c2eb

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  python:
    entry_points:
      - fastmcp = fastmcp.cli:app

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling
    - uv-dynamic-versioning >=0.7.0
    - pip
  run:
    - python >=${{ python_min }},<4
    - typing_extensions
    - pydantic-core
    - httpcore
    - starlette
    - pydocket
    - cryptography
    - pydantic-settings
    - fastmcp-slim ==3.3.0
  run_constraints:
    - openai >=1.102.0
    - anthropic >=0.48.0
    - azure-identity >=1.16.0
    - google-genai >=1.18.0
    - prefab-ui >=0.18.0
    - pydocket >=0.19.0
    - pydantic-monty ==0.0.11
    - pyjwt >=2.12.0

tests:
  - python:
      imports:
        - fastmcp
      pip_check: true
      python_version: ${{ python_min }}.*
  - requirements:
      run:
        - python ${{ python_min }}.*
        - pip
    script:
      - fastmcp --help

about:
  summary: The fast, Pythonic way to build MCP servers and clients.
  homepage: https://github.com/jlowin/fastmcp
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - rxm7706
    - dbast
    - moritzwilksch
